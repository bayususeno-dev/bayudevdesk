import NoteDetail from "@/components/NoteDetail/NoteDetail";

export default function ReactNote() {
  return (
    <NoteDetail title="React" description="Catatan dasar tentang React.">
      <h2>Apa itu React?</h2>
      <p>
        React adalah library JavaScript yang digunakan untuk membangun antarmuka
        pengguna (UI) yang interaktif dan responsif.
      </p>
    </NoteDetail>
  );
}
